- name: Create storage directory
  file:
    path: "{{compose_photoprism_data_root}}/storage"
    state: directory
    mode: '0755'
    owner: "{{compose_photoprism_uuid}}"
    group: "{{compose_photoprism_guid}}"
  become: true

- name: Create database directory
  file:
    path: "{{compose_photoprism_data_root}}/database"
    state: directory
    mode: '0755'
    owner: "{{compose_user}}"
    group: "{{compose_user}}"
  become: true
