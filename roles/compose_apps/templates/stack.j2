#!/bin/bash

{% if not compose_merge_apps %}
echo "No containers defined, nothing to do"
{% else %}
{{ compose_commands[compose_runtime] }}{% if compose_runtime == 'incus' and compose_incus_storage_pool | default('detect') != 'detect' %} --storage-pool {{ compose_incus_storage_pool }}{% endif %} {% for item in compose_merge_apps %} -f {{ compose_project_root }}/{{ item }}/compose.yaml {% endfor %} "$@"
{% endif %}
